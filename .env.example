# Base URL of the OmniMind control plane API. Defaults to https://origox.xyz
# if unset — only set this to point at a different backend (e.g. local dev).
NEXT_PUBLIC_OMNIMIND_API="https://origox.xyz"
